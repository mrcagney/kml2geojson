notifications:
  email: false
language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
  - "3.9"
# Install dependencies
before_install:
- pip install poetry
install:
- poetry install -v

# Run tests
script: pytest
